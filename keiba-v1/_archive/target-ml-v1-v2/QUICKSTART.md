# 機械学習 クイックスタートガイド

**最速で機械学習を始めるための5ステップ**

---

## ステップ1: 環境セットアップ（5分）

```powershell
# プロジェクトディレクトリに移動
cd E:\share\KEIBA-CICD\_keiba\keiba-cicd-core\KeibaCICD.TARGET\ml

# 必要なライブラリをインストール
pip install -r requirements.txt

# Jupyter Notebook起動
jupyter notebook
```

**確認**: ブラウザが開き、Jupyter Notebookのホーム画面が表示される

---

## ステップ2: はじめてのノートブック（10分）

1. Jupyter Notebookで `notebooks/00_getting_started.ipynb` を開く

2. セルを上から順に実行（Shift + Enter）

3. すべてのセルが正常に実行されることを確認

**学ぶこと**:
- ライブラリのインポート
- common.jravanから馬のデータ取得
- 機械学習の基本概念

---

## ステップ3: データを見てみる（30分）

**新しいノートブックを作成**: `notebooks/my_first_exploration.ipynb`

```python
# セル1: ライブラリインポート
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 日本語フォント設定
plt.rcParams['font.sans-serif'] = ['MS Gothic']
plt.rcParams['axes.unicode_minus'] = False
```

```python
# セル2: データ読み込み（※実際のデータパスに変更）
df = pd.read_csv("../data/race_results.csv")

# 基本情報
print(f"データ件数: {len(df)}")
print(f"カラム: {df.columns.tolist()}")
df.head()
```

```python
# セル3: 着順の分布を見る
plt.figure(figsize=(10, 6))
sns.countplot(data=df, x='着順')
plt.title('着順の分布')
plt.show()
```

```python
# セル4: 目的変数を作る
df['target'] = (df['着順'] <= 3).astype(int)
print(df['target'].value_counts())
```

**やってみよう**:
- 人気と着順の関係を見てみる
- 馬場状態別の分布を見てみる
- 自分で気になるデータを可視化してみる

---

## ステップ4: はじめての予測モデル（30分）

**新しいノートブックを作成**: `notebooks/my_first_model.ipynb`

```python
# セル1: データ準備
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import accuracy_score, roc_auc_score

# データ読み込み
df = pd.read_csv("../data/race_results.csv")
df['target'] = (df['着順'] <= 3).astype(int)

# 特徴量と目的変数
feature_cols = ['age', 'weight', 'distance', 'popularity']
X = df[feature_cols].fillna(0)
y = df['target']

print(f"特徴量: {len(feature_cols)}個")
print(f"データ件数: {len(X)}")
```

```python
# セル2: データ分割
X_train, X_test, y_train, y_test = train_test_split(
    X, y, test_size=0.3, random_state=42
)

print(f"学習データ: {len(X_train)}件")
print(f"テストデータ: {len(X_test)}件")
```

```python
# セル3: モデル訓練
model = LogisticRegression(max_iter=1000)
model.fit(X_train, y_train)

print("✓ モデル訓練完了!")
```

```python
# セル4: 予測と評価
y_pred = model.predict(X_test)
y_pred_proba = model.predict_proba(X_test)[:, 1]

accuracy = accuracy_score(y_test, y_pred)
auc = roc_auc_score(y_test, y_pred_proba)

print(f"正解率: {accuracy:.1%}")
print(f"AUC: {auc:.3f}")
```

**おめでとうございます！** 🎉
あなたは今、はじめての機械学習モデルを作りました。

---

## ステップ5: 本格的な学習開始（2週間）

ここから[**LEARNING_PLAN.md**](./LEARNING_PLAN.md)に従って、段階的に学習を進めます。

### Phase 0-5を順番に進める

| Phase | 内容 | 期間 |
|-------|------|------|
| 0 | 環境構築と基礎理解 | 1日 |
| 1 | データ理解と可視化 | 2-3日 |
| 2 | 特徴量エンジニアリング | 3-4日 |
| 3 | はじめての機械学習モデル | 2-3日 |
| 4 | モデル評価とチューニング | 3-4日 |
| 5 | バックテストと運用 | 2-3日 |

### 学習のペース配分

**推奨スケジュール（平日1時間、週末3時間の場合）**:

```
Week 1:
  月: Phase 0 完了
  火-金: Phase 1（データ探索）
  土日: Phase 2 開始（特徴量エンジニアリング）

Week 2:
  月-金: Phase 2 完了、Phase 3 開始
  土日: Phase 3 完了（はじめてのモデル）

Week 3:
  月-金: Phase 4（モデル評価）
  土日: Phase 5 開始（バックテスト）

Week 4:
  月-金: Phase 5 完了
  土日: 卒業課題（総合演習）
```

---

## 💡 学習のコツ

### 1. 手を動かす

理論を読むだけでなく、必ずコードを実行してください。
エラーが出ても大丈夫。エラーから学ぶことが多いです。

### 2. 小さく始める

最初は小さなデータセット（100件程度）で試すと、処理が速くて学習しやすいです。

### 3. 可視化する

グラフを描いて、データの傾向を目で見て確認しましょう。

### 4. メモを取る

気づいたことや疑問をノートブックにメモとして残しましょう。

### 5. 繰り返す

一度で完璧に理解する必要はありません。
何度も繰り返すうちに理解が深まります。

---

## 🔧 よくある質問

### Q1: ライブラリのインストールでエラーが出る

**A**: 以下を試してください：

```powershell
# Pythonのバージョン確認（3.8以上が必要）
python --version

# pipをアップグレード
python -m pip install --upgrade pip

# 個別にインストール
pip install pandas numpy scikit-learn lightgbm matplotlib seaborn jupyter
```

### Q2: Jupyter Notebookが起動しない

**A**:

```powershell
# Jupyterを再インストール
pip uninstall jupyter
pip install jupyter

# 起動
jupyter notebook
```

### Q3: 日本語が文字化けする

**A**:

```python
import matplotlib.pyplot as plt

# Windows
plt.rcParams['font.sans-serif'] = ['MS Gothic']

# Mac
# plt.rcParams['font.sans-serif'] = ['Hiragino Sans']

plt.rcParams['axes.unicode_minus'] = False
```

### Q4: データがない

**A**: LEARNING_PLAN.mdのPhase 1で、サンプルデータの作成方法を説明しています。
実際のJRA-VANデータがない場合は、まずダミーデータで学習を進めてください。

### Q5: どれくらいの時間がかかる？

**A**: 1日1-2時間で、約2週間が目安です。
週末に集中して取り組めば、1週間でも可能です。

---

## 📚 次のステップ

クイックスタートを完了したら：

1. **[LEARNING_PLAN.md](./LEARNING_PLAN.md)** で詳細な学習プランを確認
2. **notebooks/** で実際にコードを書きながら学習
3. **scripts/** で本番用のコードを実装

---

## 🎯 最終目標

このモジュールを完了すると：

✅ 機械学習の基礎概念を理解できる
✅ 競馬データから特徴量を設計できる
✅ 予測モデルを訓練できる
✅ モデルを評価・改善できる
✅ バックテストで回収率を計算できる
✅ 実運用可能な予測システムを構築できる

---

**それでは、ステップ1から始めましょう！** 🚀

有意義な学習になることを願っています。

---

*作成日: 2026-01-30*
