# 調教データ集計スクリプト 使い方ガイド

## 📁 ファイル構成

```
KeibaCICD.TARGET/
├── docs/
│   ├── training_summary_使い方.md        # このファイル
│   └── 調教データ作成スクリプト開発.md   # 仕様書
└── scripts/
    └── training_summary.py               # メインスクリプト

調教データ（データフォルダ）:
├── chokyo_YYMMDD_坂路.csv       # 坂路調教データ（入力）
├── chokyo_YYMMDD_コース.csv     # コース調教データ（入力）
└── output_YYMMDD.txt            # 出力ファイル
```

---

## 🚀 基本的な使い方

### コマンド形式

```bash
python training_summary.py --sakamichi 坂路.csv --course コース.csv --date YYYYMMDD --output output.txt
```

### 短縮形

```bash
python training_summary.py -s 坂路.csv -c コース.csv -d 20251228 -o output.txt
```

---

## 📋 オプション一覧

| オプション | 短縮形 | 必須 | 説明 |
|:---|:---:|:---:|:---|
| `--sakamichi` | `-s` | △ | 坂路CSVファイルのパス |
| `--course` | `-c` | △ | コース調教CSVファイルのパス |
| `--date` | `-d` | ○ | 基準日（YYYYMMDD形式） |
| `--output` | `-o` | ○ | 出力ファイルのパス |
| `--clip-lap` | - | - | 馬名・調教ラップをクリップボードにコピー |
| `--clip-time` | - | - | 馬名・調教タイムをクリップボードにコピー |
| `--clip-detail` | - | - | 馬名・調教詳細をクリップボードにコピー |
| `--excel` | - | - | Excelファイル(.xlsx)で出力 |

※ `--sakamichi` と `--course` は最低どちらか1つは必須

---

## 🔧 実行例

### 例1: 坂路とコース両方を使用

```bash
python training_summary.py \
  --sakamichi chokyo_251228_坂路.csv \
  --course chokyo_251228_コース.csv \
  --date 20251228 \
  --output output_251228.txt
```

### 例2: 坂路のみ使用

```bash
python training_summary.py \
  --sakamichi chokyo_251228_坂路.csv \
  --date 20251228 \
  --output output_251228.txt
```

### 例3: コースのみ使用

```bash
python training_summary.py \
  --course chokyo_251228_コース.csv \
  --date 20251228 \
  --output output_251228.txt
```

### 例4: クリップボードに調教ラップをコピー

```bash
python training_summary.py \
  -s chokyo_251228_坂路.csv \
  -c chokyo_251228_コース.csv \
  -d 20251228 \
  -o output.txt \
  --clip-lap
```

---

## 📊 分類記号の意味

### 調教ラップ分類（SS〜D）

| 分類 | Lap2（ラスト2F） | Lap1（ラスト1F） | 説明 |
|:---:|:---|:---|:---|
| SS | 11秒台以下 | 11秒台以下 | 最高（好タイム+S+加速or同タイム） |
| S | 11秒台以下 | 11秒台以下 | 好ラップ（2F連続11秒台以下） |
| A | 12秒台以上 | 11秒台以下 | 好（加速して終い11秒台以下） |
| B | 12秒台 | 12秒台 | 良（2F連続12秒台） |
| C | 11秒台or13秒台 | 12秒台 | 普通（終い12秒台） |
| D | - | 13秒台以上 | 軽め |

### 加速/減速（+, -, =）

| 記号 | 意味 | 条件 |
|:---:|:---|:---|
| + | 加速 | Lap2 > Lap1 |
| - | 減速 | Lap2 < Lap1 |
| = | 同タイム | Lap2 == Lap1 |

### 調教タイム分類（全角1文字）

| 記号 | 意味 |
|:---:|:---|
| 坂 | 坂路で好タイム |
| コ | コースで好タイム |
| 両 | 坂路+コース両方で好タイム |
| (ブランク) | 好タイムなし |

### 好タイム基準（4F）

| 場所 | 種別 | 基準 |
|:---|:---|:---|
| 美浦 | 坂路 | 52.9秒以下 |
| 栗東 | 坂路 | 53.9秒以下 |
| 美浦 | コース | 52.2秒以下 |
| 栗東 | コース | 52.2秒以下 |

---

## 📅 日付の考え方

### 基準日（--date）

レース開催日を指定します。例えば `20251228`（12月28日）を指定した場合：

| 分類 | 対象日 | 説明 |
|:---|:---|:---|
| 最終追い切り | 12/24（水）〜12/25（木） | 基準日直近の水・木 |
| 一週前追い切り | 12/17（水）〜12/18（木） | 最終の7日前 |

### 水・木以外の追い切り

月・火・金・土・日の追い切りは、**SS, S, A ランクの場合のみ**出力されます。

出力例：
```
最終坂路B+ (月)坂路A+
```

---

## 📄 出力ファイル形式

### フォーマット

- **区切り**: タブ区切り
- **文字コード**: Shift-JIS（CP932）
- **ヘッダー**: あり

### カラム

```
馬名	調教師	調教ラップ	調教タイム	調教詳細
```

### 出力例

```
馬名	調教師	調教ラップ	調教タイム	調教詳細
アイスアンドスノー	古賀慎明	B+		最終コースB+ 一週前コースB+
アウェイクネス	前川恭子	A+	コ	最終なし 一週前コースA+(51.8)
アスクエジンバラ	福永祐一	SS	坂	最終坂路SS(52.1) 一週前コースD-
```

---

## 🎯 TARGETへの取り込み

1. 出力ファイル（`.txt`）を生成
2. TARGETを起動
3. `ファイル` → `外部データ取り込み` を選択
4. 出力ファイルを選択
5. 取り込み完了

※ 文字コードはShift-JISなのでTARGETで直接読めます

---

## ⚠️ エラー対処

### ファイルが見つからない

```
Error: 坂路CSVファイルが見つかりません: path/to/file.csv
```

→ ファイルパスを確認してください

### 必須パラメータ不足

```
Error: --date と --output は必須です
```

→ 必須オプションを指定してください

### データ行のスキップ

```
Warning: 不正なデータ行をスキップ: ...
```

→ CSVの該当行にデータ不備があります（処理は続行されます）

---

## 📝 よくある質問

### Q: 坂路とコース両方のデータがある馬はどう表示される？

A: 調教詳細に両方記載されます。調教ラップ分類は優先度が高い方が採用されます。

例：
```
最終坂路SS(52.1) コースB+ 一週前坂路C+
```

### Q: 同じ日に複数の追い切りがある場合は？

A: 優先度（点数）が高い方が採用されます。

### Q: 最終追い切りがない馬はどうなる？

A: 一週前の追い切りが調教分類に採用されます。調教詳細には「最終なし」と表示されます。

例：
```
最終なし 一週前コースB+
```

### Q: 水木に追い切りがない馬は？

A: 直近2回の追い切りデータが調教詳細に表示されます。

例：
```
(土)坂路A+(52.5) (日)コースB+
```

---

## 🔄 バージョン履歴

| 日付 | バージョン | 変更内容 |
|:---|:---:|:---|
| 2025/12/27 | 1.0 | 初版作成 |
| 2025/12/27 | 1.1 | 分類記号を半角2文字に変更（↑→+, ↓→-, →→=） |
| 2026/01/10 | 2.0 | SS/S分類追加、調教タイム分類追加、5カラム出力対応 |
| 2026/01/16 | 2.1 | KeibaCICD.TARGETに移動 |
