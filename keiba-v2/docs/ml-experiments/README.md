# ML実験レポート

バージョンアップのたびに詳細な実験レポートを作成。
命名規則: `v{バージョン}_{短い英語名}.md`

## 実験一覧

| バージョン | 実験名 | Session | 主な結果 |
|-----------|--------|---------|---------|
| v5.5 | [Bootstrap CI + Pruning](v5.5_bootstrap_ci_pruning.md) | 44 | win_only CI=[60-170%], pruning 20%は悪化 |

## ROI推移（win_only bet_engine）

```
v5.4.1  ███████████████████ 119.3%  (366bet, CI=[60-170%])
```

> Bootstrap CIにより、366件のbet数ではROIの不確実性が非常に大きいことが判明。
> バージョン間の5-10ppの差は統計的にほぼ無意味。

## マイルストーン

- **v5.5 (Session 44)**: Bootstrap CI実装 → バックテストROIの不確実性を定量化。特徴量pruning 20%は悪化を確認。

## 学び

- **測定基盤を先に整備**: ROI改善を追う前に、その改善が統計的に有意かを判定する仕組みが必要
- **Pruningは保守的に**: importance下位20%一括削除はWin VBを-27pp悪化。dead features(=0)のみが安全
- **レース単位リサンプリング**: Bootstrap CIはレース内の馬を一括でリサンプリングすべき（独立性の仮定）
