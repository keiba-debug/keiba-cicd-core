# 今後の開発候補

## 自動投票・購入管理 UI（Session 135 追加 / ふくだ要望）

### 購入履歴・購入候補 確認画面
- **当日の購入履歴一覧**: 受付番号 / レース / 馬券種 / 馬番 / 金額 / 時刻 / (結果)
  - 手動購入 (ふくだ) とツール自動購入を**統合表示** → バンクロール会計の一元化
    （現状ツールの per_day_max は自動分しか数えない盲点を UI で吸収）
- **購入候補のリアルタイム表示**: freebudget 候補を発走/締切/投票時刻 + あと何分 + 推奨馬・金額で一覧
  （`freebudget_race --list` の Web 版。投票WINDOW入りをハイライト）
- データソース: `purchase_ledger/YYYY-MM-DD.json` + `freebudget_bets*.json` +
  `freebudget_scheduler_state.json` + IPAT 履歴（受付番号突合）
- 既存 `web/src/components/bankroll/DailyPurchaseList` を拡張する案

### レース結果の音声通知（Session 135 / ふくだ要望）
- レース確定後に結果を音声で読み上げ:「◯◯レース、◯番は◯着、ハズレ」/「的中！払戻◯◯円」
- 当日の収支を逐次アナウンス（例: 「現在 ◯勝◯敗、収支 +◯◯円」）
- 実装の前提 = **ledger v2 settle 拡張**（払戻 / SETTLED event 取得）+ 自動突合
  （[13_AUTO_RECONCILIATION.md](../auto-purchase/13_AUTO_RECONCILIATION.md) / JRA-VAN・mykeibadb のレース確定データと受付番号の突合）
- 既存 `notify.py`（VOICEVOX/SAPI）の発話基盤を流用。投票完了通知の対になる「結果通知」レイヤ
- **★結果で声を使い分け（ふくだ案 Session 135）**: ぞん子のスタイルだけで完結する案 —
  通常/的中=実況風(ID93) / ハズレ=低血圧(ID91、 脱力で逆に面白い) / 万馬券=覚醒(ID92)。
  notify.py に「文脈→speaker」 マップを足す（VOICEVOX speaker 切替の土台は Session 135 で
  実装済 = `_try_voicevox(text, speaker=)`、 既定話者は env `KEIBA_VOICEVOX_SPEAKER`）。
  ※ Session 135 時点の既定 = ぞん子 実況風(ID93) / VOICEVOX 落ち時は SAPI 低めトーンへ自動フォールバック

## Web UI 改善

### レース詳細画面にJRDB由来データ統合
- IDM（独自指数）を馬情報に表示
- JRDB SED/KYI由来のデータ（前走IDM、総合指数等）
- レース画面で市場情報との比較が可能に

### 馬成績画面にJRDB由来データ統合
- 過去走ごとのIDM推移
- JRDB独自指標のトレンド表示
- 能力変化の可視化

### IDM比較画面の拡張
- **コース/距離フィルタ**: 同一コース条件のIDMだけを抽出して比較
- **ペースフィルタ**: Hペース/Mペース/Sペース別のIDM傾向
- **IDM比較 → 過去同一重賞比較**: 例えば弥生賞のIDM推移を過去5年分重ねて「今年のメンバーレベル」を把握
  - X軸: 各年のレース日、Y軸: IDM → 年ごとの線で比較
  - 同一レース名でdata3から過去開催を自動検索

### ラップ比較画面（IDM比較のアーキテクチャ転用）
- **同一コース過去レースのラップ重ね比較**: X軸=ハロン区間、Y軸=ラップタイム
- 過去の弥生賞ラップと今年の想定ペースを重ねてレースイメージを掴む
- 凡例ON/OFF + ハイライト連動はIDM比較と同一パターン
- JRDB SED通過順位データとの連携で馬別ラップも可能に

### レース比較ビューアー（コース別レース検索 + 映像連動）
- **同一コース条件のレースを一覧比較**: コース/距離/馬場/クラスでフィルタ → カード形式で一覧表示
- 各カードに表示する情報:
  - ラップタイム（前半3F/後半3F）+ ラップグラフ
  - ペース分析（H/M/S + RPCI）
  - レースランクレベル（IDM平均/ARd平均でメンバーの質を数値化）
  - 着順Top3 + 脚質 + 着差
  - 通過順位（JRDB SED由来）
- **レーシングビューアー一括再生**: 選択したレースの映像を連続再生（JRA公式リンク or ローカル映像連携）
- **活用シーン**:
  - 「中山1200m外A 良」の過去10Rを並べて逃げ/差し有利の傾向を把握
  - 同一重賞の過去5年分を比較してメンバーレベル・ペース傾向を分析
  - 特定馬の同コース過去走を並べて成長/衰退を映像で確認
- 既存のラップ比較画面・IDM比較画面のアーキテクチャを転用可能

## 新シミュレーション

### 厳選3連単勝負シミュレーション
- 3連単を1点〜10点に絞って買い続けた場合の収支シミュレーション
- W/P/ARモデルの予測を組み合わせて3連単の軸・ヒモを選定
- ポイント:
  - 軸馬の選定方法（rank_w 1位? AR偏差値最大?）
  - ヒモ馬の選定方法（rank_p上位? EV順?）
  - フォーメーション設計（1着固定 or 1-2着固定）
  - backtest_cacheから3連単結果を取得する仕組みが必要
  - mykeibadbの3連単払戻データ連携

## 運用改善

### JRDB データダウンロード実行ボタン（Web UI）
- 管理画面からワンクリックでJRDBデータ（SED/KYI/KAA）を最新化
- ダウンロード進捗表示、完了後にindex再構築を自動トリガー
- 現状は `python -m jrdb.downloader --type SED --latest` をCLI実行

## ML モデル改善（残対応リスト）
- EV購入プラン策定 + バンクロールシミュレーション
- dam_*特徴量のPIT化検討
- Closingモデル改善（KAA外差し + ペース崩壊特徴量追加）
- 過去走IDM補正（外差し/前崩れ時のIDM割引/割増）
- LambdaRank(B-0), Sharpe比追加, 自動投票

### 予測確信度 (Prediction Confidence)
- レースごとに「モデルがどれだけ自信を持っているか」を数値化
- 活用例:
  - 確信度低いレースは見送り推奨 → 無駄な馬券購入を回避
  - 確信度高いレースに資金を集中 → バンクロール効率の最大化
  - Bank画面に確信度カラム追加、フィルタ条件として使用
- 指標候補:
  - P/Wモデルの予測確率の分散（Top1とTop2の差が大きい = 確信度高）
  - 出走馬の過去走データ充実度（初昇級馬が多い = 確信度低）
  - ARd標準偏差（馬間の差が小さい = 確信度低）
  - 同一クラス・条件の過去モデル精度（未勝利→1勝クラス昇級戦は精度低い傾向）
- 背景: 前走未勝利勝ち上がり組ばかりのレースなど、比較データが乏しいケースでモデルの限界を明示する武器になる

## コースリプレイ・エンジン発展構想（Session 186 追加 / ふくだ発案）

Session 186 で実装した CourseReplay エンジン（コース模式図 + キーフレーム + rAF経路アニメ、
予想隊列図/結果リプレイ・答え合わせで共用）の発展候補。

### ダビスタ風 実況テキスト生成（ローカルLLM）
- エンジンのフレーム間差分（先頭交代 / 何番手上がった / 差を詰めた=伸び / 隊列崩壊）が
  **そのまま実況の構造化素材**になる → LLM プロンプトをデータ駆動で組める
- JRDBコメント・脚質プロファイル・レース名/クラスを混ぜた「根拠つき実況」まで発展可
- リプレイ再生と同期してテキストを流す（将来は音声合成=ローカルTTSも視野）
- 別プロジェクト規模。KAZEMACHI(配信)との親和性も高い

### TARGET 勝負服画像でマーカー補完
- TARGET が持つ勝負服画像をマーカー円に切り抜き表示（帽色リングで囲って視認性維持）
- 画像パスの解決と Web への静的配信経路の設計が必要

### スタート〜1角のポジション争い表示
- コースパスは 1-2角+スタート直線まで延長済み（COURSE_ANCHORS.startStraight 定義済み）
- レース距離 → 発走地点マッピング（コース情報詳細）を持ち込み、
  テン指数(pred_ten_idx)+枠順から序盤隊列を合成キーフレームで生成（MLゴールと同じ手法）
- 新潟1000m直線(千直)専用の直線レイアウトもここで対応

### 風・環境レイヤー
- 風データ取得（書籍棚卸し候補C=風バイアスと合流）→ コース図に風向き矢印オーバーレイ
- 「直線追い風」「向こう正面向かい風」が一目でわかる → 上がりタイム解釈・A-1ベンチ被験者
- 馬場含水率・バイアス（内外どちらが伸びるか）も同じレイヤー機構に乗せられる

### 答え合わせρの蓄積分析 【✅Session 187 で蓄積側実装済み】
- 結果リプレイの隊列一致度(Spearman ρ: JRDB予想/ML vs 実着順)を日次で蓄積
  - ✅ `ml/collect_rho.py` + `data3/ml/rho_history.json`（2026年1-6月 1,628R バックフィル済）
  - 残: 週次prepへの組み込み（結果集計後に `python -m ml.collect_rho --date <土日>`）と可視化ダッシュボード
- 「JRDB展開予想はどのコース/クラスで当たるか」→ A-4荒れ度モデルの特徴量・目的変数候補
- 予想ゴールと実ゴールの乖離が大きい馬のパターン分類 → 残対応「大穴馬要因分析」と接続

## リプレイv2 構想（Session 187 追加 / ふくだ発案・相談確定）

結果リプレイ（ResultTenkaiReplay）の次期バージョン。優先順に:

### v2コア 【✅2026-07-03 実装済】
1. **スタートコマ（結果側）** ✅
   - `COURSE_ANCHORS.startStraight` にゲートコマ追加（`CourseFrameDef.gate` フラグ新設）
   - 位置=枠順のゲート横一列（全馬 diff=0・内外=馬番=ゲート番号で内→外に均等配置。
     ゲートコマは前後ずらし無効で全馬が同じ線上=本物のゲートの見た目。
     マーカーはゲートで見やすい0.55倍を全コマ共通サイズに採用=`MARKER_SCALE`）
   - 出遅れ演出: 実測のSED出遅補正(`jrdb_deokure`>0)の馬がいるレースは「発走直後」の
     隠し中間コマ(`CourseFrameDef.hidden`・▶再生専用)を挟み、出遅れ馬だけゲート一列から
     1.75馬身ガクッと下がる動きで表現。テロップ「スタート: ⑨が出遅れ」も表示
     （`is_slow_start`=keibabookの事前「出遅れ癖」フラグは結果側では不使用。
     速報版SEDの週末はdeokure未実測=演出なし→確定SED反映後の再生成で出る）
   - スタート→1角のアニメで「誰がダッシュしたか」が動きで見える
2. **再生速度・区間の可視化** ✅
   - 🐢速度切替（ゆっくり×2/▶普通/⏩速い×0.5）— 予想隊列図・結果リプレイ共通（エンジン側実装）
   - コマ間ポーズ 0.6→1.1秒 + 区間テロップをインフィールド中央に表示
     （通常区間=2番手以上上げた馬 最大2頭「3角→4角: ⑬ 5位→2位↑」/ ゲート区間=隊列形成「⑦が先頭・④ 2番手」/
     最終区間は再生終了後も1.6秒余韻）
   - 伸び流線は S186 時点で既に全 inPlay 区間対応済みと判明（スタート→1角は全馬 diff=0 起点なので流線なし=仕様通り）
3. **内外レーンの確率的推定**（ふくだ経験則・同日追加） ✅
   - 実測コース取り(`jrdb_course_tori`)が無い馬のレーンを「順位%3の機械散らし」から経験則ベースへ:
     ①同じような位置取りの集団は内枠の馬が内（近接順位3頭グループ内を枠番順に内→外）
     ②残り3F(3角)以降に直前コーナーから2番手以上順位を上げた馬は大外（まくり）
   - 実測がある馬は実測優先。進路データは現ソースに存在しないため、あくまで確率的近似（凡例に明記）
   - 速報版SED（コース取り全0）の週末はレース全体がこの推定で描画される

### v2続編（コアの次）
2.5 **コース別スタート位置調整（ふくだデータ提供予定・2026-07-03 追加）**
   - 現状は全レースが `COURSE_ANCHORS.startStraight`（スタンド前東側）固定＝模式図として嘘がある
     （例: 芝1200mは実際は向こう正面発走。今はコーナーコマが3角4角しか無いことで違和感が薄まっているだけ）
   - コース（場×トラック種別×距離）ごとに発走地点を持ち、スタートコマの anchor と
     スタート→最初のコーナーのアニメ経路を正しい区間から始める
   - **データ形式（案）**: JSON `data3/masters/course_start_points.json`
     ```json
     {
       "函館_芝_1200": { "segment": "back",       "t": 0.15, "note": "向こう正面半ばやや2角寄り" },
       "東京_芝_1600": { "segment": "back",       "t": 0.05, "note": "向こう正面入口(2角奥ポケット)" },
       "中山_芝_1800": { "segment": "standFront", "t": 0.5 },
       "新潟_芝_1000": { "segment": "straightOnly" }
     }
     ```
     - `segment`: `standFront`(スタンド前直線) / `c12`(1-2角) / `back`(向こう正面) /
       `c34`(3-4角) / `home`(最終直線=ゴールと同じ直線) / `straightOnly`(千直専用レイアウト)
     - `t`: 区間内の位置 0〜1（0=区間の入口=ゴールから遠い側）。ポケット発走は t≒0 + note で表現
     - エンジン側: segment+t → ゴール残距離(anchor) 変換関数を COURSE_ANCHORS に追加するだけ
   - 千直（新潟1000m直線）はコーナー無しの専用直線レイアウトもここで対応（発展構想から昇格）
   - 発展構想「スタート〜1角のポジション争い表示」（テン指数×枠順の合成キーフレーム=予想側）の土台にもなる
3. **リプレイメモ（ユーザー入力「ここで不利」）**
   - スタートメモ（EarlyPositionComparison の start-memo API）と同じパターンで
     race_id×馬番×区間キーのメモ保存。プリセット（挟まれた/外に振られた/進路なし等）+自由入力
   - リプレイの馬マーカーに⚠バッジ表示
   - **SED不利補正（S187統合済）を横に並べて「機械の見立て vs 自分の見立て」を突合** → 回顧の資産化
4. **区間ペース注釈**: `laps.lap_times`(200m刻み)をコーナー位置に対応付けて
   「向こう正面12.2→3角から11.5 ペースアップ」テロップ。設計=距離→コース位置マッピング
5. **一行実況（ルールベース）**: 1角1位=逃げ/4角先頭/上がり最速/コース取り大外×順位上昇=まくり 等。
   SED horse_pace・寸評と整合チェック可。実況LLM（上記構想）の前段。溜まったらLLM化

### 不採用（データ無し）
- 「コース中央の空きスペースを使った」等の進路レベル情報: JRA-VAN/JRDBに進路データは存在しない
  （馬単位のコース取り1値と通過順位が上限）。映像解析/トラッキングデータが必要で現ソースでは不可。
  近似は「コース取り×順位変動」の一行実況まで
